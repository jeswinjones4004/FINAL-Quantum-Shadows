[build]
  base = "frontend"
  publish = "dist"
  command = "npm install && npm run build"

[functions]
  directory = "../netlify/functions"

[[redirects]]
  from = "/api/scan"
  to = "/.netlify/functions/scan"
  status = 200
